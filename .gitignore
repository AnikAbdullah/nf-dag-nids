# OS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.hypothesis/
.coverage

# Data & generated artifacts (regenerate via scripts/notebooks, not stored in git)
data/
artifacts/
_handoff/

# Large generated binaries (papers, slide decks, zips) — keep locally, not in git
*.zip
*.pdf
*.pptx
*.xlsx

# Planning / handoff / status docs — not pushed to GitHub
*.md
!README.md
plans/

# Manuscript working area: the paper under submission, third-party journal
# templates (Wiley .cls/.sty and bundled licensed fonts), and downloaded
# copyrighted articles. None of this belongs in a public code repository.
Journal exchange/

# Local-only Claude Code settings
.claude/settings.local.json
